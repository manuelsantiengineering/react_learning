import React from 'react';


class UseCallbackTest extends React.Component {

    render() {
        return(
            <div className="container">
                <h4> Hooks: useCallback Example </h4>
                <p>It is used to memoize a function."</p>
                <p>Memoization is a technique used to primarly speed up computer programs by storing results</p>
                <p>of expensive function calls and returning the cached result when the same inputs occur again.</p>
                <p>NOT IMPLEMENTED</p>
            </div>
        );
    }
}

export default UseCallbackTest;